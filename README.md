# Neural-operator surrogates for outdoor acoustics (LEE–FDTD)

This repository accompanies the paper “Towards Neural-operator Surrogates for Outdoor Acoustics using Parameterized Wind Profiles and Terrain” (JASA submission; currently under review).

We learn operators that map parameterized wind profiles $U(z)$ and/or terrain profiles $h(x)$ to complex acoustic pressure fields on a fixed 2D region-of-interest (ROI) grid:

- $G_U: U \mapsto p(x,z)$
- $G_h: h \mapsto p(x,z)$
- $G_{U,h}: (U,h) \mapsto p(x,z)$

All models predict two output channels $[p_{\mathrm{re}},\; p_{\mathrm{im}}]$ on the ROI grid; SPL is derived by postprocessing.

---

## Repository layout

```
.
├── train_fno_wind_coords.py
├── train_fno_terrain_eta_mask_coords.py
├── train_fno_joint_wind_terrain_eta_mask.py
├── train_deeponet_wind_gridtrunk.py
├── train_deeponet_terrain_gridtrunk_eta_mask.py
└── data_sample/
    ├── sample_wind.h5
    ├── sample_terrain.h5
    └── sample_joint.h5
```

Note: if your terrain DeepONet file is currently named `train_deeponet_terrain_gridtrunk_eta_mask.py.py`, rename it to `train_deeponet_terrain_gridtrunk_eta_mask.py`.

`data_sample/` contains small HDF5 examples intended to validate that the pipeline runs end-to-end. They are not intended to reproduce the quantitative results in the manuscript.

---

## Data and representation

### ROI grid and complex pressure
Training data are generated by a 2D LEE finite-difference time-domain (FDTD) solver and stored as a complex phasor on the ROI grid using two channels:
- `pre = Re(P)`
- `pim = Im(P)`

### Terrain features on a fixed grid
Terrain is represented on the fixed ROI grid using a height-above-ground coordinate:

$$
\eta(x,z)=z-h(x)
$$

and supervision/evaluation is restricted to the air region using the mask:

$$
m(x,z)=\mathbf{1}[\eta(x,z)\ge 0].
$$

When used as a feature, $\eta$ is scaled to $\eta_{\mathrm{norm}}$. Spatial coordinates are normalized to $[-1,1]$.

### Wind features on a fixed grid
Wind is provided as a vertical profile $U(z)$ and broadcast onto the ROI grid as $U_{\mathrm{grid}}(x,z)=U(z)$, then scaled to $U_{\mathrm{norm}}$ by a constant factor.

### Direct vs residual targets 
Some scripts support:
- Direct: $y(x,z)=p(x,z)$
- Residual: $y(x,z)=\Delta p(x,z)=p(x,z)-p_0(x,z)$, with reconstruction $p=\Delta p + p_0$

The fixed baseline $p_0$ can be obtained once from the solver using no-wind $U(z)\equiv 0$ and flat ground $h(x)\equiv 0$.

---

## Scripts

### FNO (grid-to-grid)
- `train_fno_wind_coords.py`  
  Wind-only FNO. Input channels are wind-on-grid (and optionally $dU/dz$) plus normalized coordinates.

- `train_fno_terrain_eta_mask_coords.py`  
  Terrain-only FNO. Input channels are $\eta_{\mathrm{norm}}$, air mask $m$, and normalized coordinates. Loss/metrics can be masked to the air region.

- `train_fno_joint_wind_terrain_eta_mask.py`  
  Joint wind+terrain FNO. Input channels include wind-on-grid, $\eta_{\mathrm{norm}}$, air mask, and coordinates (optionally $dU/dz$ and a visibility channel, depending on configuration).

### DeepONet with grid trunk (FNO-style trunk)
- `train_deeponet_wind_gridtrunk.py`  
  Wind-only DeepONet. Branch encodes $U(z)$ via sensors; trunk is a grid-based spectral (FNO-like) network over $(x,z)$.

- `train_deeponet_terrain_gridtrunk_eta_mask.py`  
  Terrain-only DeepONet. Branch encodes $h(x)$ via sensors; trunk uses grid coordinates with optional $\eta$ and mask features; outputs are masked to the air region.

---

## Running

Each script has a `CFG` block near the top. To run on the included samples:

1) Set `CFG["data_root"]` to `data_sample/` (or point it to your own dataset folder).
2) Run the script, e.g.
```bash
python train_fno_wind_coords.py
```

Scripts typically write checkpoints and evaluation summaries into per-run folders (names depend on `RUN_TAG` and the script).

---

## Notes on reproducibility and data availability

- The included sample HDF5 files are intended for sanity checks only (structure, I/O, training loop).
- The full Sobol-sampled HDF5 datasets used to produce the paper results are not included in this repository. They are available from the authors upon reasonable request.

---

## Citation

This repository accompanies the paper “Towards Neural-operator Surrogates for Outdoor Acoustics using Parameterized Wind Profiles and Terrain,” currently under review.
